import React from 'react';
import SearchBar from './SearchBar';

// functional component App
const App = () => {
    return (
      <div>
        <SearchBar />
      </div>
    );
};

export default App;